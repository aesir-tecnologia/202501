# Supabase Configuration (Required)
# These values are embedded in the static build at build-time
# IMPORTANT: Only use public/anon keys here - never use service role keys in production!
SUPABASE_URL="https://your-project-id.supabase.co"
SUPABASE_ANON_KEY="your_supabase_anon_key_here"

# Local Development Only (for integration tests)
# Get these from `supabase status` after running `supabase start`
# WARNING: Never commit real service role keys - they bypass RLS!
SUPABASE_SERVICE_ROLE_KEY="your_local_service_role_key_here"
