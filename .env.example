# Supabase Configuration (Required)
# These values are embedded in the static build at build-time
# IMPORTANT: Only use public/anon keys here - never use service role keys!
SUPABASE_URL="https://your-project-id.supabase.co"
SUPABASE_ANON_KEY="your_supabase_anon_key_here"
